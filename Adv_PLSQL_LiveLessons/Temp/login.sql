rem column INSTANCE_ID new_value INST_ID noprint
rem select HOST_NAME || ':' || INSTANCE_NAME || ':' || USER || '> ' INSTANCE_ID
rem from sys.v_$instance
rem /
rem set SQLPROMPT &&INST_ID
set serveroutput on


